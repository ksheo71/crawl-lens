name: deploy
on:
  push: { branches: [main] }
  workflow_dispatch:
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - run: /opt/stack/services/public/myazit.kr/crawl-lens/repo/scripts/deploy.sh
